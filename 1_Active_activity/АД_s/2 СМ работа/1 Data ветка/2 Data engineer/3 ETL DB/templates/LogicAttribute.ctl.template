options (skip=1)

-- Block for table: LogicAttribute
LOAD DATA
CHARACTERSET AL32UTF8
infile '/opt/oracle/scripts/custom/data/LogicAttribute.csv'
badfile 'LogicAttribute.bad'
discardfile 'LogicAttribute.dis'
append
into table "LogicAttribute"
fields terminated by ',' optionally enclosed by '"'
trailing nullcols
(
  "ENTRY" CHAR(255),
  "GateNumber" INTEGER EXTERNAL,
  "AttributeName" CHAR(80),
  "Copy" INTEGER EXTERNAL,
  "AttributeValue" CHAR(255)
)
